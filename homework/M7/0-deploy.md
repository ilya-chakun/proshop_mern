# 0 — Local Model Deployment

> **STATUS: PENDING LIVE RUN** — this file requires a real local-model call log to
> earn the Part-0 rubric points (2 pts). The structure below is finalized; the
> transcript must be captured during the `[LIVE]` Go-Live step (T1.4 / T8.6).

## 1. Chosen path
- [ ] **A — Local Ollama** (default) · [ ] B — VPS · [ ] C — Teacher-provided endpoint

## 2. Model + explicit quant
- **Model:** `qwen3:8b-q6_K` (explicit quantization pinned).

## 3. Endpoint
- Base URL: `http://localhost:11434/v1` (Ollama OpenAI-compatible) — confirm at live run.

## 4. Hardware note
- RAM / GPU: _PENDING_ — record actual machine specs and why `qwen3:8b-q6_K` fits
  (q6_K ≈ ~6.6 GB weights; needs ~10–12 GB RAM headroom for context).

## 5. REAL call log (request + response transcript)
```
PENDING LIVE RUN — paste the real request body and model response here.
```
